- name: Remove default nginx html
  file:
    path: /var/www/html/*
    state: absent
  become: yes

- name: Copy React build to nginx folder
  copy:
    src: "{{ playbook_dir }}/../deploy/"
    dest: "{{ deploy_path }}/"
    owner: root
    group: root
    mode: "0755"
  become: yes
